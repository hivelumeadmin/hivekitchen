// @file: apps/web/src/features/auth/re-entry-dialog.tsx
// Dialog primitive used inside one of the four allowlisted feature directories.
import * as Dialog from '@radix-ui/react-dialog';

export function ReEntryDialog() {
  return <Dialog.Root />;
}
