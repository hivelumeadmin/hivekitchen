{
  "name": "@hivekitchen/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@hivekitchen/tsconfig": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.5"
  }
}
